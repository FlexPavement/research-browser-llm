{
  "name": "browser-llm-chat",
  "version": "1.0.0",
  "description": "Browser-based LLM chat application using Gemma and Transformers.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && touch dist/.nojekyll && gh-pages -d dist"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.6.0",
    "solid-js": "^1.9.3"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^6.0.7",
    "vite-plugin-solid": "^2.10.2"
  }
}
